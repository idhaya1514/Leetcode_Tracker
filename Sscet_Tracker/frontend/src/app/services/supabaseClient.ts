/**
 * Supabase has been permanently disconnected.
 * The application is now fully reliant on the Neon PostgreSQL cloud database.
 */
export const supabase = null;
export const isSupabaseConfigured = false;
